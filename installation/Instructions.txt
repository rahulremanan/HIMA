#Execute inside the redis folder:
sudo chmod +x ./redis
sudo ./redis.sh

# To remove unfinished tasks:
redis-cli flushdb

sudo pip3 install celery redis python-keystoneclient

# Execute inside Celery app folder:
celery worker -A app.celery --loglevel=info

# Run at start-up:
update-rc.d cron defaults
sudo service cron  start

update-rc.d redis defaults 100
update-rc.d app defaults 100
update-rc.d celery defaults 100

sudo chmod +x ./app
sudo chmod +x ./celery

sudo service cron start

-- Not recommended --
$ crontab -e
@reboot /home/rahul/Jomiraki/redis &
@reboot /home/rahul/Jomiraki/celery &
@reboot /home/rahul/Jomiraki/app &



